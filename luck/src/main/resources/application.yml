eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8799
spring:
  application:
    name: service-luck
#暴露所有节点DashBoard监控用的
management:
  endpoints:
    web:
      exposure:
        include: "*"
